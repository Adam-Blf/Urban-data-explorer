services:
  api:
    build:
      context: .
      dockerfile: api/Dockerfile
    ports: ["8000:8000"]
    volumes: ["./data:/app/data:ro"]

  frontend:
    image: nginx:alpine
    ports: ["5173:80"]
    volumes:
      - ./frontend:/usr/share/nginx/html:ro
    depends_on: [api]
